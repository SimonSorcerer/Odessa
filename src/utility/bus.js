define(['knockout'], function (ko) {
    'use strict';

    return function Bus() {
        function publish() {

        }

        function listen() {

        }

        return {
            publishOn: publish,
            listenTo: listen
        };
    }
});
